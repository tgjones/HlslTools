trigger:
- master

pr:
- master

jobs:
- job: HlslTools_Build_Test
  strategy:
    matrix:
      VS2017:
        imageName: 'vs2017-win2016'
        _name: VS2017
      VS2019:
        imageName: 'windows-2019'
        _name: VS2019
  pool:
    vmImage: $(imageName)
  steps:
  - task: PowerShell@2
    displayName: Build
    inputs:
      filePath: Build.ps1
      arguments: -Target Build